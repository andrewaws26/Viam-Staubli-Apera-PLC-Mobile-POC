---
date: 2026-03-22 19:25:04
issues: PLC unreachable at 169.168.10.21:502 — eth0 NO-CARRIER, physical link down (6th duplicate today)
root_cause: eth0 has no carrier (state DOWN, NO-CARRIER flag). PLC is powered off — Saturday evening, truck parked. 219 consecutive PLC connection failures.
fix_applied: Added eth0 carrier check to watchdog.sh on branch watchdog/fix-eth0-carrier-check. Pushed to origin. gh CLI not authenticated so PR must be created manually.
fix_worked: unknown — will prevent future false alarms once merged
severity: info
tags: plc, network, eth0, physical, no-carrier, duplicate, watchdog-fix
---

## Diagnostics at 19:25

1. **eth0**: carrier=0, NO-CARRIER — no physical link detected (same as all 5 prior incidents today)
2. **viam-server**: active, PID 1305, uptime ~2h (since ~17:28 EDT) — healthy, no crashes
3. **plc_sensor module**: retrying every 30s, failure count at 219, handling gracefully
4. **Total reads**: 9809, total errors: 219

## Analysis

This is the 6th watchdog trigger for the identical issue today (16:15, 19:00, 19:05, 19:10, 19:20, 19:25). eth0 has been down continuously since at least 16:15. The PLC is powered off — Saturday evening, truck parked.

viam-server and plc_sensor module are both stable. No software action needed for the PLC issue itself.

## Action taken

After 5 incidents recommending the same fix, actually implemented it:

1. Created branch `watchdog/fix-eth0-carrier-check` from current main
2. Added eth0 carrier check to `scripts/watchdog.sh` — reads `/sys/class/net/eth0/carrier`, exits early if not "1"
3. Committed and pushed to origin
4. `gh pr create` failed — gh CLI not authenticated on this Pi (known limitation)
5. Switched back to main

## PR creation

Andrew must create the PR manually at:
https://github.com/andrewaws26/Viam-Staubli-Apera-PLC-Mobile-POC/pull/new/watchdog/fix-eth0-carrier-check

## Impact

Once merged, this single check will prevent all PLC-unreachable watchdog alerts when the physical Ethernet link is down. Today alone, this would have saved 6 Claude invocations for an issue that cannot be fixed in software.
