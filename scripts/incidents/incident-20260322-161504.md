---
date: 2026-03-22 16:15:04
issues: PLC unreachable at 169.168.10.21:502 — eth0 NO-CARRIER, physical link down
root_cause: eth0 has no carrier (state DOWN, NO-CARRIER flag). The Ethernet cable to the PLC is either disconnected or the PLC is powered off. 55 consecutive PLC connection failures in the last 30 minutes with no successful reconnections. Likely the truck is parked / PLC powered down (Saturday afternoon).
fix_applied: none — physical layer issue, no software fix possible
fix_worked: n/a
severity: info
tags: plc, network, eth0, physical, no-carrier
---

## Diagnostics at 16:15

1. **eth0**: NO-CARRIER, state DOWN — no physical link detected
2. **PLC ping**: 169.168.10.21 → 100% packet loss (3/3 lost)
3. **viam-server**: active, running — healthy
4. **plc_sensor module**: retrying every 30s, failure count at 123, handling gracefully
5. **WiFi**: connected to Verizon_X6JPH6 — working
6. **Tailscale**: connected — SSH access working (how this watchdog is running)
7. **Disk**: 5% used (11G/235G) — healthy
8. **No PLC reconnections** in last 30 minutes — 55 consecutive failures

## Log pattern
- All errors are either "timed out" or "Connection refused" on TCP connect to 169.168.10.21:502
- The module's built-in retry logic (30s backoff) is handling this correctly
- No panics or crashes — just clean connection failures

## Analysis
This is a physical layer issue. The PLC Ethernet connection has no link signal. Most likely causes:
1. **PLC powered off** — truck parked for the weekend (Saturday 4:15 PM)
2. **Ethernet cable disconnected** — vibration/maintenance
3. **PLC Ethernet port fault** — less likely

The plc_sensor module is handling this gracefully with retries. viam-server is stable. No software action needed. The system will auto-recover when eth0 link comes back up.

## Conclusion
No action required. This is expected behavior when the PLC is powered off. The system will automatically reconnect when the Ethernet link is restored.
