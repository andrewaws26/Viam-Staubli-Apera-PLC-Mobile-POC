---
date: 2026-03-22 04:30:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 401st consecutive alert
root_cause: eth0 NO-CARRIER — Ethernet cable disconnected or PLC powered off. No IP on eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 2s. Internet via wlan0 working. viam-server active (PID 17976).
fix_applied: none — physical layer issue, requires human intervention
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Alert suppression — 401st identical alert

No new information since incident 170504. eth0 down ~2086 minutes (~34.8 hours).

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER = no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back.

## OverflowError still present

The `OverflowError - int too large to convert to float` at `_on_connection_failure` line 318 is still firing every ~20s. The fix exists on branch `watchdog/fix-backoff-overflow` (pushed to origin) but the PR cannot be created because `gh auth login` has not been run. Andrew needs to either:
1. Run `gh auth login` on the Pi so the watchdog can create PRs, or
2. Manually create the PR from the GitHub web UI for branch `watchdog/fix-backoff-overflow`

## Context

End-of-day PLC shutdown (weekend). No action needed until next work session.
