---
date: 2026-03-20 17:25:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 5th consecutive alert for same physical disconnect since ~16:59
root_cause: Same as incidents 170504, 171004, 171505, 172004. Kernel NO-CARRIER flag on eth0 persists since dmesg timestamp 17840s (~16:59 EDT). Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 30s, failure count now ~51 (was 42 at 17:20). Internet via wlan0 working fine (8.8.8.8: 38-165ms, 0% loss). viam-server PID 17976 active.
fix_applied: none — physical layer issue, requires human intervention. NOT restarting viam-server (would not help and would cause startup transient errors triggering false-alarm watchdog cycle)
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Diagnostics at 17:25

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged since ~16:59)
2. **eth0 IP**: none assigned (should be 169.168.10.x/24)
3. **PLC ping**: 169.168.10.21 → 100% packet loss (2/2 lost)
4. **viam-server**: active, PID 17976 — running
5. **plc_sensor module**: running, 9 connection failures in last 5 minutes, failure count ~51
6. **Internet**: ping 8.8.8.8 → 38-165ms, 0% loss — wlan0 working
7. **dmesg**: "Link is Down" at timestamp 17840s (~16:59), no subsequent "Link is Up" message

## This is the 5th consecutive watchdog alert for the same physical disconnect

- 17:05: eth0 NO-CARRIER detected, failure count 14
- 17:10: failure count 24
- 17:15: failure count 33
- 17:20: failure count 42
- 17:25: failure count ~51 (this alert)

## Why no action was taken

- eth0 NO-CARRIER = Ethernet PHY detects no link partner — this is Layer 1 (physical)
- No software action (restart viam-server, restart NetworkManager, etc.) can restore a physical link
- The plc_sensor module is handling the outage correctly with 30s retry backoff
- Module will auto-reconnect when cable/PLC is restored — no restart needed

## Context

eth0 link has been down for ~26 minutes (since ~16:59 EDT / ~5:00 PM). This is almost certainly an end-of-day PLC shutdown — the link went down right around quitting time. No action needed until next work session.

## Recurring recommendation (27th time)

The watchdog needs to suppress repeated alerts for ongoing physical disconnects. Five identical alerts in 20 minutes for the same unplugged cable is noise, not signal.
