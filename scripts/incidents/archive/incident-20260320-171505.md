---
date: 2026-03-20 17:15:05
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), continuation of 17:05 and 17:10 incidents
root_cause: Same as incident-20260320-170504 and incident-20260320-171004. Kernel NO-CARRIER flag on eth0 persists since dmesg timestamp 17840s (~16:59 EDT). Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 30s, failure count now 33 (was 24 at 17:10, 14 at 17:05). Internet via wlan0 working fine (8.8.8.8: 151-195ms, 0% loss). viam-server PID 17976 running with ~52 min uptime.
fix_applied: none — physical layer issue, requires human intervention. NOT restarting viam-server (would not help and would cause startup transient errors triggering false-alarm watchdog cycle)
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Diagnostics at 17:15

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged since ~16:59)
2. **eth0 IP**: none assigned (should be 169.168.10.x/24)
3. **eth0 route**: no route to 169.168.10.0/24
4. **PLC ping**: 169.168.10.21 → 100% packet loss (2/2 lost)
5. **viam-server**: active, PID 17976, uptime ~52 min (since 16:23:44) — running
6. **plc_sensor module**: running, retrying every 30s, failure count 33 (up from 24 at 17:10)
7. **Internet**: ping 8.8.8.8 → 151-195ms, 0% loss — wlan0 working well
8. **wlan0**: UP on Verizon_X6JPH6 (192.168.0.176)
9. **dmesg**: "Link is Down" at timestamp 17840s (~16:59), no subsequent "Link is Up" message

## This is the 3rd consecutive watchdog alert for the same physical disconnect

- 17:05: eth0 NO-CARRIER detected, failure count 14
- 17:10: eth0 NO-CARRIER persists, failure count 24
- 17:15: eth0 NO-CARRIER persists, failure count 33 (this alert)

## Why no action was taken

- Restarting viam-server would NOT restore eth0 physical link
- Restarting NetworkManager would NOT restore eth0 physical link (NO-CARRIER = Ethernet PHY detects no link partner)
- The plc_sensor module is handling the outage correctly with 30s exponential backoff retries
- Module will auto-reconnect when the cable is restored — no restart needed
- A restart would cause startup transient errors and risk triggering the false-alarm watchdog cycle (documented in 16+ previous incidents today)

## Action required (human intervention)

1. Check that the Ethernet cable is firmly plugged into both the Pi's eth0 port and the PLC's Ethernet port
2. Verify PLC power — PWR and RUN LEDs should be green
3. Try a different Ethernet cable if available
4. Once link is restored, eth0 will auto-negotiate and plc_sensor will reconnect automatically

## Context

This is likely an end-of-day PLC shutdown (link went down at ~16:59 EDT / ~5:00 PM). If the truck is done for the day, this is expected behavior and no action is needed until the next work session.

## Recurring recommendation (25th time)
1. **Merge the `watchdog/fix-false-alarms` branch** to stop false alarm alerts
2. The watchdog should recognize NO-CARRIER as a physical issue and suppress repeated alerts for the same ongoing outage
3. Consider adding a "known physical disconnect" state so the watchdog doesn't keep firing every 5 minutes for the same cable unplug
