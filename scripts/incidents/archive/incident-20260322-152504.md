---
date: 2026-03-22 15:25:04
issues: PLC unreachable — eth0 physical link down (NO-CARRIER), continuation of post-recovery recurrence
root_cause: eth0 NO-CARRIER — Ethernet cable disconnected or PLC powered off. PLC had recovered at ~08:00 today after 38h weekend downtime, operated ~7 hours, now down again (likely end of work session).
fix_applied: none — physical layer issue, requires human intervention
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Alert suppression — continuation of eth0 NO-CARRIER

Same issue as previous alert. eth0 down since ~15:15 today (~10 minutes).

## What was checked

- `ip link show eth0`: NO-CARRIER, state DOWN
- `ping 169.168.10.21`: 100% packet loss
- `systemctl is-active viam-server`: active
- plc_sensor module retrying every ~30s (failures at 31 and climbing)

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER = no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back, as it did successfully at 08:00 today.

## System health

- viam-server: active (PID 17976)
- Internet: wlan0 working
- 18 errors in 5 min — all "Connection to (169.168.10.21, 502) failed: timed out" — expected while eth0 is down

## Pending

- OverflowError fix on branch `watchdog/fix-backoff-overflow` still needs PR/review (gh auth blocked)
