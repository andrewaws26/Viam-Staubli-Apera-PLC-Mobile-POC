---
date: 2026-03-20 19:20:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 28th consecutive alert for same physical disconnect since ~16:59
root_cause: Same as incidents 170504 through 191505. Kernel NO-CARRIER flag on eth0 persists since ~16:59 EDT. Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 30s, failure count now ~262. Internet via wlan0 working. viam-server active.
fix_applied: none — physical layer issue, requires human intervention. Implementing alert suppression in watchdog script via PR.
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Diagnostics at 19:20

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged since ~16:59)
2. **PLC ping**: not attempted — no route to 169.168.10.0/24 with eth0 down
3. **viam-server**: active (PID 17976)
4. **plc_sensor module**: running, failure count ~262, retry interval 30s

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER means the Ethernet PHY detects no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back — no restart needed.

## Context

eth0 down ~141 minutes. End-of-day PLC shutdown. No action needed until next work session.

## Action: Implementing alert suppression NOW

This is the 28th identical alert. Implementing alert suppression in the watchdog script via a PR. After 3 consecutive alerts for the same root cause, the watchdog will reduce frequency to once per hour or suppress until state changes.
