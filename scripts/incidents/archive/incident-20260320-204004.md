---
date: 2026-03-20 20:40:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 33rd consecutive alert for same physical disconnect since ~16:59
root_cause: Same as incidents 170504 through 200504. Kernel NO-CARRIER flag on eth0 persists since ~16:59 EDT. Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 30s, failure count ~414. Internet via wlan0 working. viam-server active.
fix_applied: none — physical layer issue, requires human intervention
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Diagnostics at 20:40

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link
2. **viam-server**: active (PID 17976)
3. **plc_sensor module**: running, failure count ~414, retry interval 30s

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER = no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back.

## Context

eth0 down ~221 minutes. End-of-day PLC shutdown. No action needed until next work session.

## Alert suppression — 51st recommendation

This is the 33rd identical alert in ~221 minutes. The watchdog script needs alert suppression: after 3 consecutive alerts for the same root cause (eth0 NO-CARRIER), suppress until state changes or reduce to once per hour.
