---
date: 2026-03-20 14:30:02
issues: viam-server cloud connection stuck — Go gRPC cached EOF errors after Verizon DNS "server misbehaving" episode
root_cause: Verizon router DNS (192.168.0.1) returned "server misbehaving" starting ~14:28, then transitioned to "error reading server preface: EOF" — indicating TCP connections to app.viam.com were established but TLS/HTTP2 handshake failed due to extreme Verizon WiFi latency (1.7-2s to 8.8.8.8, 337-756ms to app.viam.com). Go's gRPC client cached the failed connection state and did not recover. Shell DNS resolved app.viam.com fine throughout. Errors persisted for 3+ minutes at ~16 errors per 30 seconds. Local data capture continued normally (225KB .prog file growing).
fix_applied: sudo systemctl restart viam-server at 14:31:24
fix_worked: true
severity: warning
tags: viam-server, cloud-sync, dns, network, restart, verizon, gRPC-stuck
---

## Diagnostics at 14:30
1. **viam-server**: active, PID 10077, uptime ~29 min (since 14:01:20) — running but cloud-disconnected
2. **Internet**: ping 8.8.8.8 → 1753-2004ms, 0% loss — reachable but extreme latency
3. **Viam Cloud**: ping app.viam.com → 337-756ms, 0% loss — reachable
4. **PLC ping**: 169.168.10.21 → 0.162-0.168ms, 0% loss — healthy
5. **Capture**: .prog file 225KB at 14:30, actively growing — local data pipeline working
6. **Cloud sync**: OFFLINE, exponential backoff at 1m0s intervals
7. **DNS**: resolv.conf has 192.168.0.1 (Verizon router), shell DNS resolves fine
8. **eth0**: UP with PLC (169.168.10.1/24)
9. **wlan0**: UP on Verizon_X6JPH6 (192.168.0.176)

## Error timeline
- ~14:28:00 — DNS "server misbehaving" errors begin (Verizon router DNS)
- ~14:29:47 — Errors transition to "error reading server preface: EOF" (TCP connects but TLS fails)
- 14:29:50 — Data sync goes OFFLINE with exponential backoff
- 14:30:24+ — Errors continue every 5 seconds, no self-recovery
- 14:31:04 — Still 16 errors per 30 seconds — confirmed stuck (3+ min duration)
- 14:31:24 — Restarted viam-server (new PID 11723)
- 14:31:35 — DNS tests: 3/3 connection, 4/4 resolution passed (slow: cloudflare/google >1000ms)
- 14:31:42 — STUN: 7/7 UDP + 2/2 TCP passed
- 14:31:58 — Cloud connection established (34 seconds after restart)
- 14:32:41 — WebRTC succeeded via LAN peer (192.168.0.170:62017)

## Key differentiator from false alarms
- PID 10077 was 29 min old — NOT startup transient
- Errors persistent for 3+ minutes with no sign of self-recovery
- Shell DNS worked but viam-server's Go resolver/gRPC was stuck
- 16 errors per 30 seconds, continuous

## Pattern: 4th real DNS/gRPC failure on Verizon_X6JPH6 today
- 13:07–13:10: DNS stuck → restart fixed
- 13:35–13:36: DNS stuck → restart fixed
- 14:00: DNS stuck + 66% packet loss → restart fixed
- 14:30: DNS → EOF stuck → restart fixed (this incident)

All on Verizon_X6JPH6 WiFi. The common pattern: Verizon router DNS fails briefly, Go's resolver/gRPC caches the failure, and when errors persist >2-3 min, self-recovery doesn't happen.

## Recurring recommendation (19th time)
1. Merge the `watchdog/fix-false-alarms` branch to stop false alarm alerts
2. Verizon WiFi continues to be severely degraded — switch to B&B Shop WiFi if available
3. Consider adding systemd-resolved with fallback DNS (8.8.8.8, 1.1.1.1) to bypass Verizon router DNS failures
