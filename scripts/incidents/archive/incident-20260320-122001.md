---
date: 2026-03-20 12:20:01
issues: No capture data in last 5 minutes, high error rate (47 errors) — false alarm, old process panicked, new process healthy
root_cause: viam-server PID 1420 panicked at 12:17:06 (PanicCapturingGoWithCallback in goroutine 773, likely during data capture). systemd automatically restarted viam-server as PID 2683 at 12:17:06. The 47 errors counted by the watchdog include panic stack traces from the dying process (PID 1420) and normal startup warnings from the new process (PID 2683) — meta.json parse skip, deprecated validate_config signature, data_manager race condition. All are benign. The new process connected to PLC within 3 seconds and began capturing data.
fix_applied: none — viam-server self-restarted via systemd, all subsystems recovered automatically
fix_worked: true
severity: info
tags: viam-server, panic, restart, capture, false-alarm, watchdog
---

## Timeline
- 12:17:06 — viam-server PID 1420 panicked (PanicCapturingGoWithCallback in goroutine 773)
- 12:17:06 — systemd started new viam-server instance PID 2683
- 12:17:07 — Module loaded (meta.json warning — expected for local modules)
- 12:17:08 — data_manager configured, plc-monitor resource constructed successfully
- 12:17:08 — Capture collector initialized at 1Hz, sync workers started (pool size 2)
- 12:17:08 — PlcSensor configured: host=169.168.10.21 port=502
- 12:17:09 — 🟢 PLC connected at 169.168.10.21:502
- 12:17:08 — 7/7 UDP STUN tests passed
- 12:17:08 — WebRTC connection #1 succeeded (172.20.10.6 ↔ relay 34.203.251.60)
- 12:17:12 — 2/2 TCP STUN tests passed
- 12:17:16 — WebRTC connection #2 succeeded (srflx 172.59.80.51 ↔ srflx 75.192.74.57)
- 12:17:19 — WebRTC connection #3 failed (context deadline exceeded) — stale attempt from restart window, normal
- 12:20:01 — Watchdog triggered
- 12:20:20 — .prog file actively growing (8.2KB) — capture confirmed active

## Diagnostics at investigation time (~12:20)
1. **PLC ping**: 169.168.10.21 → 0.160ms avg, 0% packet loss — healthy
2. **eth0**: UP, LOWER_UP, carrier=1, state UP — fully operational
3. **viam-server**: active, PID 2683, started 12:17:06, uptime ~3 min
4. **Capture**: .prog file at 12:20:20 is 8.2KB and growing — data flowing
5. **PLC module**: connected, no errors after 12:17:09
6. **Cloud sync**: WebRTC connected, STUN tests passing, 2 sync workers active
7. **No errors in journal after startup** — clean operation

## Error breakdown (47 errors)
- **From PID 1420 (dead process)**: panic stack traces — this is why the process died
- **From PID 2683 (new process, all benign)**:
  - meta.json parse skip — expected for local (non-tarball) modules
  - validate_config deprecated signature — cosmetic warning, module works fine
  - "Resource missing from dependencies: plc-monitor" — normal startup race, resolved by reconfigure
  - ALSA capture slave not defined — irrelevant audio subsystem warning
  - WebRTC connection timeout — stale connection attempt during restart window

## Analysis
This is the 10th watchdog trigger today. The panic in PID 1420 is new — previous incidents were caused by eth0 NO-CARRIER and PLC connection flapping. The panic occurred in the data capture goroutine, suggesting a possible race condition or nil pointer in viam-server's capture pipeline. This is an upstream viam-server issue, not a module bug.

The ~3 minute capture gap (12:17:06 to 12:20:20) is the expected restart window. The .prog file from 12:17:06 is 67B (likely just the header before the old process died), and the new .prog file at 12:20:20 is actively growing at 8.2KB.

## Capture file timeline
- 12:01:15 — 0B .prog (from earlier eth0 NO-CARRIER incident)
- 12:17:06 — 67B .prog (header only, old process panicked)
- 12:20:20 — 8.2KB .prog (active, growing — data flowing)

## Conclusion
No action required. System is fully operational. The panic-triggered restart was handled cleanly by systemd. All subsystems healthy: PLC connected (0.16ms), eth0 up, capture flowing, cloud sync active.

## Recurring recommendation (8th time)
The watchdog continues to generate false alarms during self-healing windows. The same improvements are needed:
1. **Check .prog file mtime** — if modified <30s ago, capture is active
2. **Grace period after viam-server restart** — suppress alerts for 120s post-restart
3. **Separate error counts by PID** — don't count errors from dead processes
4. **Hysteresis** — require 2+ consecutive failed checks before alerting
5. **Check viam-server uptime** — if < 5 min, extend grace period
6. **New**: Log the panic stack trace for upstream bug reporting to Viam
