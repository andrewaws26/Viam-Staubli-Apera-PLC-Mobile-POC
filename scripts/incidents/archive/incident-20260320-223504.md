---
date: 2026-03-20 22:35:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 56th consecutive alert for same physical disconnect since ~16:59
root_cause: Same as incidents 170504 through 223004. Kernel NO-CARRIER flag on eth0 persists since ~16:59 EDT. Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 30s, failure count ~625. Internet via wlan0 working. viam-server active.
fix_applied: none — physical layer issue, requires human intervention
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Diagnostics at 22:35

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged)
2. **viam-server**: active (PID 17976)
3. **plc_sensor module**: running, failure count ~625, retry interval 30s

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER = no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back.

## Context

eth0 down ~336 minutes. End-of-day PLC shutdown. No action needed until next work session.

## Alert suppression — 56th identical alert

This is the 56th identical alert in ~336 minutes. No new information since incident 170504. The watchdog script needs alert suppression for repeated identical root causes — after 3 consecutive alerts with the same root cause, suppress until state changes or reduce frequency to once per hour.
