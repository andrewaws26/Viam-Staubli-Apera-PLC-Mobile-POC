---
date: 2026-03-20 17:35:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 7th consecutive alert for same physical disconnect since ~16:59
root_cause: Same as incidents 170504, 171004, 171505, 172004, 172504, 173004. Kernel NO-CARRIER flag on eth0 persists since dmesg timestamp 17840s (~16:59 EDT). Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 30s, failure count now ~69 (was ~60 at 17:30). Internet via wlan0 working. viam-server PID 17976 active.
fix_applied: none — physical layer issue, requires human intervention. NOT restarting viam-server (would not help and would cause startup transient errors triggering false-alarm watchdog cycle)
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Diagnostics at 17:35

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged since ~16:59)
2. **eth0 IP**: none assigned (should be 169.168.10.x/24)
3. **PLC ping**: 169.168.10.21 → 100% packet loss (2/2 lost)
4. **viam-server**: active — running
5. **plc_sensor module**: running, 10 connection failures in last 5 minutes, failure count ~69

## This is the 7th consecutive watchdog alert for the same physical disconnect

- 17:05: eth0 NO-CARRIER detected, failure count 14
- 17:10: failure count 24
- 17:15: failure count 33
- 17:20: failure count 42
- 17:25: failure count ~51
- 17:30: failure count ~60
- 17:35: failure count ~69 (this alert)

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER means the Ethernet PHY detects no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back — no restart needed.

## Action required (human intervention)

1. Check that the Ethernet cable is firmly plugged into both the Pi's eth0 port and the PLC's Ethernet port
2. Verify PLC power — PWR and RUN LEDs should be green
3. Try a different Ethernet cable if available
4. Once link is restored, eth0 will auto-negotiate and plc_sensor will reconnect automatically

## Context

eth0 link has been down for ~36 minutes (since ~16:59 EDT / ~5:00 PM). This is almost certainly an end-of-day PLC shutdown. No action needed until next work session.

## Recurring recommendation (29th time)

The watchdog needs to suppress repeated alerts for ongoing physical disconnects. Seven identical alerts in 30 minutes for the same unplugged cable is noise, not signal.
