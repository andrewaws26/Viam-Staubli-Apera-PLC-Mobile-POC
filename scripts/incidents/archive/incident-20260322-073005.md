---
date: 2026-03-22 07:30:05
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 437th consecutive alert
root_cause: eth0 NO-CARRIER — Ethernet cable disconnected or PLC powered off. No IP on eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 2s. Internet via wlan0 working. viam-server active (PID 17976).
fix_applied: none — physical layer issue, requires human intervention
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Alert suppression — 437th identical alert

No new information. eth0 down ~2266 minutes (~37.8 hours).

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER = no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back.

## System health

- viam-server: active (PID 17976)
- 154 errors in 5 min — all "Connection to (169.168.10.21, 502) failed: timed out" — expected while eth0 is down
- Internet: wlan0 working

## OverflowError note

Log shows `OverflowError - int too large to convert to float` in `_on_connection_failure` line 318. Fix exists on branch `watchdog/fix-backoff-overflow` (pushed to origin). PR creation blocked — needs `gh auth login`. Andrew to review.

## Context

End-of-day PLC shutdown (weekend). No action needed until next work session.
