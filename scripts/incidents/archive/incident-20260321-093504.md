---
date: 2026-03-21 09:35:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 174th consecutive alert since ~16:59
root_cause: Same as incidents 170504 through 093005. Kernel NO-CARRIER flag on eth0 persists since ~16:59 EDT. Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 2s. Internet via wlan0 working. viam-server active.
fix_applied: none — physical layer issue, requires human intervention
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Alert suppression — 174th identical alert

This is the 174th identical alert in ~951 minutes. No new information since incident 170504.

## Diagnostics at 09:35

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged)
2. **carrier file**: `/sys/class/net/eth0/carrier` = 0
3. **viam-server**: active (PID 17976)

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER = no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back.

## Pending fix

OverflowError backoff bug fix pushed to `origin/watchdog/fix-backoff-overflow` in incident 023004. PR creation requires `gh auth login`. Andrew to review.

## Context

eth0 down ~951 minutes. End-of-day PLC shutdown. No action needed until next work session.
