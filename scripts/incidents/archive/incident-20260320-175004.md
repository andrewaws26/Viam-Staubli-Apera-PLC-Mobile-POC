---
date: 2026-03-20 17:50:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 10th consecutive alert for same physical disconnect since ~16:59
root_cause: Same as incidents 170504 through 174504. Kernel NO-CARRIER flag on eth0 persists since dmesg timestamp 17840s (~16:59 EDT). Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 30s, failure count now ~97. Internet via wlan0 working. viam-server active.
fix_applied: none — physical layer issue, requires human intervention. NOT restarting viam-server (would not help and would cause startup transient errors triggering false-alarm watchdog cycle)
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Diagnostics at 17:50

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged since ~16:59)
2. **eth0 IP**: none assigned (should be 169.168.10.x/24)
3. **PLC ping**: 169.168.10.21 → 100% packet loss
4. **viam-server**: active — running
5. **plc_sensor module**: running, failure count ~97

## This is the 10th consecutive watchdog alert for the same physical disconnect

- 17:05: eth0 NO-CARRIER detected, failure count 14
- 17:10: failure count 24
- 17:15: failure count 33
- 17:20: failure count 42
- 17:25: failure count ~51
- 17:30: failure count ~60
- 17:35: failure count ~69
- 17:40: failure count ~78
- 17:45: failure count ~88
- 17:50: failure count ~97 (this alert)

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER means the Ethernet PHY detects no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back — no restart needed.

## Context

eth0 link has been down for ~51 minutes (since ~16:59 EDT / ~5:00 PM). This is almost certainly an end-of-day PLC shutdown. No action needed until next work session.

## Recurring recommendation (32nd time)

The watchdog needs to suppress repeated alerts for ongoing physical disconnects. Ten identical alerts in 45 minutes for the same unplugged cable is noise, not signal. A suppression mechanism should be implemented: after 3 consecutive alerts for the same root cause, reduce alert frequency to once per hour or suppress until state changes.
