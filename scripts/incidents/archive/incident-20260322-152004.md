---
date: 2026-03-22 15:20:04
issues: PLC unreachable — eth0 physical link down (NO-CARRIER), 444th consecutive alert since last recovery
root_cause: eth0 NO-CARRIER — Ethernet cable disconnected or PLC powered off. PLC had recovered at ~08:00 today after 38h downtime, now down again (likely end of work session).
fix_applied: none — physical layer issue, requires human intervention
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Alert suppression — 444th alert (post-recovery recurrence)

PLC was reachable from ~08:00 to ~15:15 today (about 7 hours of operation). Now eth0 is NO-CARRIER again.

## What was checked

- `ip link show eth0`: NO-CARRIER, state DOWN
- `ping 169.168.10.21`: 100% packet loss
- `systemctl is-active viam-server`: active
- plc_sensor module retrying every ~30s (failures 13→22 in the log window)

## Why no action was taken

No software action can restore a physical link. eth0 NO-CARRIER = no link partner at Layer 1. The plc_sensor module will auto-reconnect when the link comes back, as it did successfully at 08:00 today.

## System health

- viam-server: active (PID 17976)
- Internet: wlan0 working (this report is being written remotely)
- ~20 errors in 5 min — all "Connection to (169.168.10.21, 502) failed: timed out" — expected while eth0 is down

## Pending

- OverflowError fix on branch `watchdog/fix-backoff-overflow` still needs PR/review (gh auth blocked)
