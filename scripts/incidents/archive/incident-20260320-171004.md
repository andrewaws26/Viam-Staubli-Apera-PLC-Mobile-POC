---
date: 2026-03-20 17:10:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), continuation of 17:05 incident
root_cause: Same as incident-20260320-170504. Kernel NO-CARRIER flag on eth0 persists since dmesg timestamp 17840s (~16:59 EDT). Ethernet cable disconnected from Pi or PLC, or PLC powered off. No IP assigned to eth0, no route to 169.168.10.0/24. plc_sensor module retrying every 30s, failure count now 24 (was 14 at 17:05). Internet via wlan0 working (8.8.8.8: 182-406ms, 0% loss).
fix_applied: none — physical layer issue, requires human intervention. NOT restarting viam-server (would not help and would cause startup transient errors)
fix_worked: true
severity: warning
tags: plc, network, eth0, physical, hardware, continuation
---

## Diagnostics at 17:10

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged since 16:59)
2. **eth0 IP**: none assigned (should be 169.168.10.x/24)
3. **eth0 route**: no route to 169.168.10.0/24
4. **PLC ping**: 169.168.10.21 → 100% packet loss (2/2 lost)
5. **viam-server**: active, PID 17976, uptime ~47 min (since 16:23:44) — running
6. **plc_sensor module**: running, retrying every 30s, failure count 24 (up from 14 at 17:05)
7. **Internet**: ping 8.8.8.8 → 182-406ms, 0% loss — wlan0 working
8. **wlan0**: UP on Verizon (192.168.0.176)
9. **dmesg**: "Link is Down" at timestamp 17840s, no subsequent "Link is Up" message

## This is a continuation of incident-20260320-170504

The eth0 link has been down for ~11 minutes (since ~16:59). The plc_sensor module is handling this correctly with 30s retry backoff. No software action can fix a Layer 1 (physical) issue.

## Why no action was taken

- Restarting viam-server would not restore eth0 physical link
- Restarting NetworkManager would not restore eth0 physical link (NO-CARRIER = no link partner detected by PHY)
- The module will auto-reconnect when the cable is restored — no restart needed
- A restart would cause startup transient errors and risk triggering the false-alarm watchdog cycle

## Action required (human intervention)

1. Check that the Ethernet cable is firmly plugged into both the Pi's eth0 port and the PLC's Ethernet port
2. Verify PLC power — PWR and RUN LEDs should be green
3. Try a different Ethernet cable if available
4. Once link is restored, eth0 will auto-negotiate and plc_sensor will reconnect automatically

## Recurring recommendation (24th time)
1. **Merge the `watchdog/fix-false-alarms` branch** to stop false alarm alerts
2. This physical disconnect is likely end-of-day shutdown — PLC may have been powered off intentionally
3. The watchdog should recognize NO-CARRIER as a physical issue and not trigger restart cycles
