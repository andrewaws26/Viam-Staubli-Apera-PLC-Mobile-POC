---
date: 2026-03-21 02:30:04
issues: PLC unreachable — eth0 physical link still down (NO-CARRIER), 97th consecutive alert; OverflowError in backoff calculation
root_cause: (1) eth0 NO-CARRIER since ~16:59 EDT — PLC powered off or cable disconnected. (2) OverflowError at plc_sensor.py:318 — 2**(consecutive_failures-1) overflows float when failures > ~1024.
fix_applied: Pushed branch watchdog/fix-backoff-overflow with exponent cap (max 5). PR creation failed (gh CLI not authenticated) — Andrew needs to create PR manually or run gh auth login.
fix_worked: unknown — PR not yet merged/deployed; eth0 still physically down
severity: warning
tags: plc, network, eth0, physical, hardware, continuation, overflow, backoff, code-fix
---

## Diagnostics at 02:30

1. **eth0**: `<NO-CARRIER,BROADCAST,MULTICAST,UP>` state DOWN — no physical link (unchanged since ~16:59)
2. **carrier file**: `/sys/class/net/eth0/carrier` = 0
3. **viam-server**: active (PID 17976)
4. **plc_sensor module**: running, failure count ~1033, retry interval 30s

## OverflowError bug found and fix pushed

The log shows:
```
OverflowError - int too large to convert to float - func_name='_on_connection_failure' line_num=318
```

Root cause: `_INITIAL_BACKOFF_SECONDS * (2 ** (self._consecutive_failures - 1))` — when `consecutive_failures` > ~1024, `2**1023+` is a Python bigint that can't convert to float for the `min()` comparison.

Fix: cap the exponent at 5 (`2**5 = 32`, already clamped to `_MAX_BACKOFF_SECONDS = 30.0`). Pushed to `origin/watchdog/fix-backoff-overflow`. `gh pr create` failed because gh CLI not authenticated — Andrew needs to create PR from GitHub or run `gh auth login` first.

## eth0 status

97th consecutive alert. ~571 minutes of downtime. End-of-day PLC shutdown. No software action can restore physical link. Module will auto-reconnect when link returns.

## Alert suppression

No new network information since incident 170504. Only new finding is the OverflowError bug.
