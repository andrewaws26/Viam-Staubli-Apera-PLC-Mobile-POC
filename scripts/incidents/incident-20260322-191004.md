---
date: 2026-03-22 19:10:04
issues: PLC unreachable at 169.168.10.21:502 — eth0 NO-CARRIER, physical link down (4th duplicate today)
root_cause: eth0 has no carrier (state DOWN, NO-CARRIER flag). Ethernet cable to PLC is disconnected or PLC is powered off. 191 consecutive PLC connection failures. Truck parked for the weekend (Saturday evening).
fix_applied: none — physical layer issue, no software fix possible
fix_worked: n/a
severity: info
tags: plc, network, eth0, physical, no-carrier, duplicate
---

## Diagnostics at 19:10

1. **eth0**: NO-CARRIER, state DOWN — no physical link detected (same as 16:15, 19:00, and 19:05 incidents)
2. **viam-server**: active, PID 1305, uptime 1h42m (since ~17:28 EDT) — healthy, no crashes
3. **plc_sensor module**: retrying every 30s, failure count at 191, handling gracefully
4. **Total reads**: 8828, total errors: 191 (2.16% error rate overall)

## Analysis

This is the 4th watchdog trigger for the identical issue today (16:15, 19:00, 19:05, 19:10). eth0 has been down continuously since at least 16:15. The PLC is powered off — Saturday evening, truck parked.

viam-server and plc_sensor module are both stable. No restarts, no panics. The module's 30s retry backoff is working as designed.

## Conclusion

No action required. System will auto-recover when PLC is powered on / Ethernet link restored.

## Critical watchdog improvement needed

The watchdog has now triggered 4 times today for an issue that cannot be fixed in software. Each invocation costs an LLM call that produces the same conclusion. The watchdog script MUST add an early eth0 carrier check:

```bash
if [[ $(cat /sys/class/net/eth0/carrier 2>/dev/null) != "1" ]]; then
    echo "eth0 has no carrier — PLC physically unreachable, skipping alert"
    exit 0
fi
```

This single check would have prevented all 4 PLC-related watchdog triggers today. A branch with watchdog fixes (including this check) was previously pushed to `watchdog/fix-false-alarms` but has not been merged.
